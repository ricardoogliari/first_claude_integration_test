{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case $f in *.ts|*.tsx) npx next lint --file \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Linting..."
          }
        ]
      }
    ]
  }
}
